import * as React from "react";
import { createWrapperComponent } from "./create-wrapper-component";

const FlexContainer = createWrapperComponent({ className: "flex-container" });

export {
    FlexContainer
}
